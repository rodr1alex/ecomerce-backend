server:
  port: 8085

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/rodr1alex/ecomerce-backend.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
