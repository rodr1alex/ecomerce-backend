server:
  port: 8083
spring:
  application:
    name: carrito-service
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/carrito_service?useSSL=false&serverTimezone=UTC
    username: rodr1alex
    password: 1234

eureka:
  client:
    service-url.defaultZone: http://localhost:8761/eureka

